# HexStrike AI MCP Agents v6.0 - Core Dependencies
#
# These are the minimal dependencies required for the HexStrike AI platform.
# For offensive tooling dependencies (pwntools, angr), use requirements-offensive.txt
#
# INSTALLATION:
# pip install -r requirements-core.txt

# ============================================================================
# CORE FRAMEWORK DEPENDENCIES
# ============================================================================
flask>=2.3.0,<4.0.0             # Web framework for API server
flask-cors>=4.0.0,<7.0.0        # CORS support for frontend development
requests>=2.31.0,<3.0.0         # HTTP library
psutil>=5.9.0,<6.0.0            # System utilities
fastmcp>=0.2.0,<1.0.0           # MCP framework for AI agent communication

# ============================================================================
# WEB SCRAPING & AUTOMATION
# ============================================================================
beautifulsoup4>=4.12.0,<5.0.0   # HTML parsing
selenium>=4.15.0,<5.0.0         # Browser automation
webdriver-manager>=4.0.0,<5.0.0 # ChromeDriver management

# ============================================================================
# ASYNC & NETWORKING
# ============================================================================
aiohttp>=3.8.0,<4.0.0           # Async HTTP client

# ============================================================================
# PROXY & TESTING
# ============================================================================
mitmproxy>=9.0.0,<11.0.0        # HTTP proxy

# ============================================================================
# PRODUCTION SERVER (Optional - for --production mode)
# ============================================================================
gunicorn>=21.0.0,<23.0.0        # Production WSGI server
uvicorn>=0.27.0,<0.35.0         # ASGI server for async support
